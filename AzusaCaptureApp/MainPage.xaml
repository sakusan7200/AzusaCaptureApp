<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AzusaCaptureApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzusaCaptureApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d">

    <Grid>
        <tb:TaskbarIcon x:Name="taskbaricon" ToolTipText="AzusaCaptureApp">
            <tb:TaskbarIcon.IconSource>
                <tb:GeneratedIconSource Text="🖼️" Foreground="Red"/>
            </tb:TaskbarIcon.IconSource>
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout AreOpenCloseAnimationsEnabled="False">
                    <MenuFlyoutItem Text="ウィンドウを表示" Command="{Binding ReturnToMainWindowCommand}"/>
                    <MenuFlyoutItem Text="今すぐキャプチャ" Command="{Binding StartCaptureCommand}"/>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem Text="完全終了" Command="{Binding ExitAppCommand}"/>
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
        <Grid x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="40px"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <CommandBar x:Name="cmdBar" Grid.Row="0" Grid.Column="0" Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
                <AppBarButton Icon="Save" Command="{Binding SaveToCommand}" Label="保存"/>
                <AppBarButton Icon="Copy" Command="{Binding SetClipboardCommand}" Label="コピー"/>
                <AppBarButton Icon="OpenWith" Command="{Binding OpenInAnotherAppCommand}" Label="ほかのプログラムで開く"/>
                <AppBarButton Icon="Trim" Command="{Binding StartTrimCommand}" Label="トリミング"/>
                <AppBarSeparator/>
                <AppBarButton Icon="Add" Label="すぐキャプチャ" Command="{Binding StartCaptureCommand}"/>
                <AppBarButton Icon="Add" Label="キャプチャ2" Command="{Binding StartCapture2Command}"/>
                <AppBarSeparator/>
                <AppBarButton Icon="Setting" Label="環境設定" Command="{Binding OpenSettingCommand}"/>
            </CommandBar>
            <CommandBar x:Name="trimCmdBar" Visibility="Collapsed" Grid.Row="0" Grid.Column="0" Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
                <AppBarButton Icon="Trim" Command="{Binding FinishTrimCommand}" Label="完了"/>
            </CommandBar>
            <ScrollViewer Grid.Row="1" Grid.Column="0" ZoomMode="Enabled"
            IsTabStop="True" IsVerticalScrollChainingEnabled="True"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
            HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Auto"
            VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Background>
                    <SolidColorBrush Color="#2d2c2c"/>
                </ScrollViewer.Background>
                <Grid x:Name="imgContent">
                    <Image x:Name="img" Source="{Binding CurrentImgSource}" AutomationProperties.Name="cliff" Stretch="None"
           HorizontalAlignment="Center" VerticalAlignment="Center"/>


                    <Image x:Name="fullSizeImg" Source="{Binding FullSizeImgSource}" Visibility="Collapsed" AutomationProperties.Name="cliff" Stretch="None"
           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Canvas Visibility="Collapsed" x:Name="cnv" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Thumb x:Name="TopLeftThumb" Canvas.ZIndex="10" Width="10" Height="10" Background="Blue"/>
                        <Thumb x:Name="TopRightThumb" Canvas.ZIndex="10" Width="10" Height="10" Background="Blue"/>
                        <Thumb x:Name="BottomLeftThumb" Canvas.ZIndex="10" Width="10" Height="10" Background="Blue"/>
                        <Thumb x:Name="BottomRightThumb" Canvas.ZIndex="10" Width="10" Height="10" Background="Blue"/>
                        <Rectangle x:Name="selectionAreaRectangle" Canvas.ZIndex="1" Stroke="Aqua" StrokeThickness="6" Fill="Transparent" Opacity="1" Canvas.Left="0" Canvas.Top="0" />

                        <Rectangle x:Name="LeftOverlay" Canvas.ZIndex="1" Opacity="0.7" Fill="Black" Canvas.Left="0" Canvas.Top="0" />
                        <Rectangle x:Name="RightOverlay" Canvas.ZIndex="1" Opacity="0.7" Fill="Black" Canvas.Left="0" Canvas.Top="0" />
                        <Rectangle x:Name="TopOverlay" Canvas.ZIndex="1" Opacity="0.7" Fill="Black" Canvas.Left="0" Canvas.Top="0" />
                        <Rectangle x:Name="BottomOverlay" Canvas.ZIndex="1" Opacity="0.7" Fill="Black" Canvas.Left="0" Canvas.Top="0" />
                        
                        
                    </Canvas>
                </Grid>
            </ScrollViewer>


        </Grid>



    </Grid>
</Page>
