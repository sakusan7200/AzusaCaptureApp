<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AzusaCaptureApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzusaCaptureApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:tb="using:H.NotifyIcon"
    Title="AzusaCaptureApp">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>


    <StackPanel>
        <!--<Grid x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="40px"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <CommandBar Grid.Row="0" Grid.Column="0" Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
                <AppBarButton Icon="Save" Command="{Binding SaveToCommand}" Label="保存"/>
                <AppBarButton Icon="Copy" Command="{Binding SetClipboardCommand}" Label="コピー"/>
                <AppBarButton Icon="OpenWith" Command="{Binding OpenInAnotherAppCommand}" Label="ほかのプログラムで開く"/>
                <AppBarSeparator/>
                <AppBarButton Icon="Add" Label="キャプチャ" Command="{Binding StartCaptureCommand}"/>
                <AppBarSeparator/>
                <AppBarButton Icon="Setting" Label="環境設定"/>
            </CommandBar>
            <ScrollViewer Grid.Row="1" Grid.Column="0" ZoomMode="Enabled"
            IsTabStop="True" IsVerticalScrollChainingEnabled="True"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
            HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Auto"
            VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Background>
                    <SolidColorBrush Color="#2d2c2c"/>
                </ScrollViewer.Background>
                <Image Source="{Binding CurrentImgSource}" AutomationProperties.Name="cliff" Stretch="None"
           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ScrollViewer>


        </Grid>-->


        <!--<Button Content="aaaaaa" Click="Button_Click"/>-->

    </StackPanel>

</Window>
