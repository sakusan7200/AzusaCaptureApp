<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AzusaCaptureApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzusaCaptureApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="using:H.NotifyIcon"
    xmlns:zoom="using:ZoomViewMauiDev"
    mc:Ignorable="d">

    <Grid>
        <Grid.Resources>
            <Style x:Key="TopLeft" TargetType="Thumb">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Grid>
                                <Polygon 
                                Points="-7,-7 5,-7 5,-5 -5,-5 -5,5 -7,5" 
                                Fill="SkyBlue" 
                                StrokeThickness="2" />
                                <Polygon Points="-7,-7 5,-7 5,7 -7,7" Fill="Transparent"/>

                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        
        <tb:TaskbarIcon x:Name="taskbaricon" ToolTipText="AzusaCaptureApp">
            <tb:TaskbarIcon.IconSource>
                <tb:GeneratedIconSource Text="🖼️" Foreground="Red"/>
            </tb:TaskbarIcon.IconSource>
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout AreOpenCloseAnimationsEnabled="False">
                    <MenuFlyoutItem Text="ウィンドウを表示" Command="{Binding ReturnToMainWindowCommand}"/>
                    <MenuFlyoutItem Text="今すぐキャプチャ" Command="{Binding StartCaptureCommand}"/>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem Text="完全終了" Command="{Binding ExitAppCommand}"/>
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
        
        
        <Grid x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="43px"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TitleBar Title="AzusaCaptureApp">
                <TitleBar.Content>
                    <Grid>
                        <CommandBar x:Name="cmdBar" Grid.Row="0" Grid.Column="0" IsSticky="True" IsOpen="False" DefaultLabelPosition="Right">
                            <AppBarButton Icon="Save" IsEnabled="{Binding IsDataNonNull}" Command="{Binding SaveToCommand}" Label="保存">
                                <AppBarButton.KeyboardAccelerators>
                                    <KeyboardAccelerator Key="S" Modifiers="Control"/> 
                                </AppBarButton.KeyboardAccelerators>
                            </AppBarButton>
                            <AppBarButton Icon="Copy" IsEnabled="{Binding IsDataNonNull}" Command="{Binding SetClipboardCommand}" Label="コピー"/>
                            <AppBarButton Icon="OpenWith" IsEnabled="{Binding IsDataNonNull}" Command="{Binding OpenInAnotherAppCommand}" Label="ほかのプログラムで開く"/>
                            <AppBarButton Icon="Trim" IsEnabled="{Binding IsDataNonNull}" Command="{Binding StartTrimingCommand}" Label="トリミング"/>
                            <AppBarSeparator/>
                            <AppBarButton Icon="Add" Label="すぐキャプチャ" Command="{Binding StartCaptureCommand}"/>
                            <AppBarButton Icon="Add" Label="キャプチャ2" Command="{Binding StartCapture2Command}"/>
                            <AppBarSeparator/>
                            <AppBarButton Icon="Setting" Label="環境設定" Command="{Binding OpenSettingCommand}"/>
                        </CommandBar>
                        <CommandBar x:Name="trimCmdBar" Visibility="Collapsed" Grid.Row="0" Grid.Column="0" Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
                            <AppBarButton Icon="Trim" Command="{Binding FinishTrimCommand}" Label="完了"/>
                        </CommandBar>
                    </Grid>
                </TitleBar.Content>
            </TitleBar>

            <ScrollViewer x:Name="scrollViewer"
                ZoomMode="Enabled"
                IsTabStop="True" 
                IsVerticalScrollChainingEnabled="True"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalScrollMode="Enabled" 
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollMode="Enabled" 
                Grid.Row="1"
                VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Background>
                    <SolidColorBrush Color="#2d2c2c"/>
                </ScrollViewer.Background>
                    <Image Source="{Binding CurrentImg2}" AutomationProperties.Name="cliff" Stretch="None"
               HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ScrollViewer>
            <zoom:ZoomView x:Name="zoomView" Visibility="Collapsed" Grid.Row="1" Grid.Column="0" Img="{Binding FullSizeImg2}"></zoom:ZoomView>


        </Grid>



    </Grid>
</Page>
