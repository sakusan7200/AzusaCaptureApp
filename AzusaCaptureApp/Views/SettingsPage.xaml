<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AzusaCaptureApp.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzusaCaptureApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="mainContent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Style="{StaticResource NavigationBackButtonNormalStyle}"
                ToolTipService.ToolTip="Back"
                Click="Button_Click"
                Content="{Binding TestValue}"/>
        <Grid Grid.Row="1" Background="YellowGreen">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="設定ファイルは%AppData%...です" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Spacing="20" Background="#202020" Padding="100,20,100,0">
            <Border Padding="10" CornerRadius="10" Background="#2b2b2b" BorderBrush="#DDD">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" TextAlignment="Left" Text="保存先"/>
                    <TextBox Text="{Binding Setting.SaveTo, Mode=TwoWay}" VerticalAlignment="Center" Grid.Row="0" Grid.Column="1" TextAlignment="Left"/>
                    <Button Grid.Row="0" Grid.Column="2" Command="{Binding ShowOFDForSettingCommand}" Content="参照"/>
                </Grid>
            </Border>

            <Border Padding="10" CornerRadius="10" Background="#2b2b2b" BorderBrush="#DDD">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" TextAlignment="Left" Text="自動保存"/>
                    <ToggleSwitch IsOn="{Binding Setting.AutoSave, Mode=TwoWay}" Grid.Row="0" Grid.Column="2"/>
                </Grid>
            </Border>

            <Border Padding="10" CornerRadius="10" Background="#2b2b2b" BorderBrush="#DDD">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" TextAlignment="Left" Text="自動でクリップボードにコピー"/>
                    <ToggleSwitch IsOn="{Binding Setting.AutoCopy, Mode=TwoWay}" Grid.Row="0" Grid.Column="2"/>
                </Grid>
            </Border>

            <Border Padding="10" CornerRadius="10" Background="#2b2b2b" BorderBrush="#DDD">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" TextAlignment="Left" Text="保存時のフォーマット"/>
                    <ComboBox Grid.Row="0" Grid.Column="2" SelectedItem="{Binding Setting.DefaultFormat}" ItemsSource="{Binding AllFormats}"/>
                </Grid>
            </Border>

            <Border Padding="10" CornerRadius="10" Background="#2b2b2b" BorderBrush="#DDD">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" TextAlignment="Left" Text="Windowsログオン時の自動起動"/>
                    <!--レジストリをいじる必要あり-->
                    <ToggleSwitch IsOn="{Binding Setting.AutoCopy, Mode=TwoWay}" Grid.Row="0" Grid.Column="2"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</Page>