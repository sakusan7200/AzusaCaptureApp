<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AzusaCaptureApp.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzusaCaptureApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="mainContent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Style="{StaticResource NavigationBackButtonNormalStyle}"
                ToolTipService.ToolTip="Back"
                Click="Button_Click"/>
        <Grid Grid.Row="1" Background="YellowGreen">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock>
                        <Run Text="設定ファイルは"/>
                        <Run Text="{x:Bind local:Cont.SettingDir}"/>
                        <Run Text="にあります。"/>
                </TextBlock>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Spacing="20" Padding="100,20,100,0">
            <controls:SettingsCard Header="保存先" Description="画像の保存先">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>



                    <TextBox Text="{Binding Setting.SaveTo, Mode=TwoWay}" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" TextAlignment="Left"/>
                    <Button Grid.Row="0" Grid.Column="2" Command="{Binding ShowOFDForSettingCommand}" Content="参照"/>
                </Grid>
            </controls:SettingsCard>
            

            <controls:SettingsCard Header="自動保存" Description="キャプチャ時に、上記保存先に即時自動で保存するか">
                <ToggleSwitch IsOn="{Binding Setting.AutoSave, Mode=TwoWay}"/>

            </controls:SettingsCard>

            <controls:SettingsCard Header="自動コピー" Description="キャプチャ時に、上記保存先に即時自動で保存するか">
                <ToggleSwitch IsOn="{Binding Setting.AutoCopy, Mode=TwoWay}"/>
            </controls:SettingsCard>

            <controls:SettingsCard Header="保存時のフォーマット" Description="自動保存で保存するときのフォーマット">
                <ComboBox Grid.Row="0" Grid.Column="2" Width="100" SelectedIndex="{Binding SettingDefaultFormatIndex, Mode=TwoWay}" ItemsSource="{x:Bind local:CompatibleFormat.AllFormats}"/>

            </controls:SettingsCard>

            <controls:SettingsCard Header="ログオン時の自動起動" Description="Windowsにログオンしたときにこのアプリを自動起動するか設定します。\nご自身でレジストリを操作された場合、正常に動作しないことがあります。">
                <ToggleSwitch IsOn="{Binding Setting.AutoStart, Mode=TwoWay}" Grid.Row="0" Grid.Column="2"/>

            </controls:SettingsCard>
        </StackPanel>
    </Grid>
</Page>